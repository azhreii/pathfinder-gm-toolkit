{
  "name": "pf2e-gm-toolkit",
  "version": "0.1.0",
  "description": "Encounter builder and NPC generator for Pathfinder 2e in Foundry VTT",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --dev --watch",
    "build:validate": "node build.mjs --validate-only"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "@sentry/browser": "^8.0.0"
  }
}
